name: C/C++ CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    env: 
      APP_PATH: "SharedBuffer/module"
    steps:
    - name: Downloading the repository
      uses: actions/checkout@v1
      
    - name: Test
      run: |
        ls
        
    - name: Build module
      run: |
        cd ${{ env.APP_PATH }}
        make
        
    - name: Inserting the module and test it
      run: |
        cd ${{ env.APP_PATH }}
        sudo ls
        sudo insmod ${{ env.APP_PATH }}/SharedBuffer.ko
        lsmod | grep SharedBuffer.ko
      
    
    
