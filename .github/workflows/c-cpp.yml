name: C/C++ CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: Downloading the repository
      uses: actions/checkout@v1
      
    - name: Test
      run: |
        ls
      
    
    
